# Smoke

The no-UI path is proven by the root `headless_v1` composition smoke
(root evidence for batch 7 wave C): REST, and with it audit, install and boot
without `larena/ui`, and `Larena\Ui\Smart` is not loadable there.
