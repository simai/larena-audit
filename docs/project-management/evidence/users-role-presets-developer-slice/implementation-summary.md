# Implementation summary

The admin history now projects known Docara lifecycle events and `auth.*` / `access.*` security events through a strict payload allowlist. Unknown events and credential fields are not rendered.
