name: audit
package: larena/audit
status: contract-skeleton
edition: core
product_layer: runtime-security
license: Apache-2.0
title: Larena Audit
description: Unified structured audit layer that receives package AuditEvent objects, validates taxonomy, redacts sensitive payloads, correlates runtime activity, routes events to sinks and provides operator search/export/diagnostics.
canonical_specs:
  repository: simai/larena-specs
  commit: 7c580ae
  package_spec: specs/packages/audit/package.json
  features: specs/packages/audit/features.json
implementation:
  runtime_code_started: true
  current_batch: runtime-security/batch-1/audit
  launch_record_required: true
